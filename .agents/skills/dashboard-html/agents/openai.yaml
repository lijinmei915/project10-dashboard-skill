interface:
  display_name: "Dashboard HTML"
  short_description: "生成响应式 Dashboard 与 Report 独立 HTML"
  default_prompt: "Use $dashboard-html to create a lightweight standalone Dashboard or Report HTML with responsive layout and stable visual tokens."

policy:
  allow_implicit_invocation: true
