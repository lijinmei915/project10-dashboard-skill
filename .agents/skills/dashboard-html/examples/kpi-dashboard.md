# KPI Dashboard Example

## 用户输入

```txt
帮我做一个销售概览 dashboard，首屏有 4 个 KPI 卡片，下面是趋势图和待跟进事项。页面保持简洁，重点统一卡片和留白，并适配手机端。
```

## 期望输出

- 输出应为完整独立 HTML 页面。
- 首屏应出现 2-4 列的 KPI 卡片区。
- KPI 卡片样式应统一，包含一致的圆角、内边距和轻阴影。
- 趋势图和待办区应分成独立卡片，不和 KPI 混成一个大块。
- 小屏下 KPI 区应能降列显示。
